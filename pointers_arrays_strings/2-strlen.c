#include "main.h"

/**
 *_strlen- Function that returns the lenght of a string
 *@s: Points to char
 *Return: integer
 */

int _strlen(char *s)
{
	int i = 0;

	while
		(s[i] != '\0')i++;
	return (1);
}
